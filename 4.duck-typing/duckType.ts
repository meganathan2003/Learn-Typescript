/**
 * Duck typing means you can change the value 
 * from the object and check the data type also 
 * and check the key value in object
 * 
 */

var complextype = { name: "diensh", id: 1 };
complextype = { id: 2, name: "meganathan" };

console.log(complextype);
